# Title

Use the **Git tag** and the **Release title** below verbatim.

## Git tag

```
v1.0.0
```

Annotated + signed:

```bash
git tag -s v1.0.0 -m "EZHTML v1.0.0"
git push --tags
```

## Release title

```
EZHTML v1.0.0 — "Markdown for HTML"
```

The subtitle "Markdown for HTML" is a one-liner that fits the
description body and the Discord announcement at the same time. Cite
the subtitle whenever someone asks what v1.0.0 is about.
