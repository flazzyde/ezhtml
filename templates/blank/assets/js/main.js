// Blank template ships no JavaScript by design.
// Add your own scripts below — they run after the page is parsed because
// the index.ezhtml attaches the <script> tag with `defer`.
console.log("EZHTML blank template is alive.");
